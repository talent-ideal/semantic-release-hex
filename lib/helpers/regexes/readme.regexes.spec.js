// import { readmeVersionRegex } from "./readme.regexes.js";

/**
 * SPEC: https://github.com/talent-ideal/semantic-release-hex/issues/20
 */

describe("readmeVersionRegex", () => {
  it.todo("should match the various dependencies specification format");

  it.todo("should match the various version requirements operators");

  it.todo("should match the git tag when pointing to a git repository");

  it.todo("should error if the version requirement contains 'and' or 'or'");
});
